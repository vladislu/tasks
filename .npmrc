# Pin the public registry so a developer's private mirror never leaks into
# package-lock.json: the GitHub Pages workflow runs `npm ci` unauthenticated and
# fails on any resolved URL it cannot reach.
registry=https://registry.npmjs.org/
